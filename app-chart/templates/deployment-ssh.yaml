apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.sshapp.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.sshapp.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.sshapp.name }}
    spec:
      volumes:
      - name: {{ .Values.volume.fs.name }}
        persistentVolumeClaim:
          claimName: {{ .Values.volume.fs.name }}
      - name: {{ .Values.volume.glade.name }}
        nfs:
          server: {{ .Values.volume.glade.server }}
          path: {{ .Values.volume.glade.path }}
          readOnly: {{ .Values.volume.glade.readOnly }}
      containers:
      - name: {{ .Values.sshapp.name }}
        image: {{ .Values.sshapp.container.image.repository }}:{{ .Values.sshapp.container.image.tag }}
        resources:
          limits:
            memory: {{ .Values.sshapp.container.memory }}
            cpu: {{ .Values.sshapp.container.cpu }}
        ports:
        - containerPort: {{ .Values.sshapp.container.port }}
        volumeMounts:
        - mountPath: {{ .Values.volume.fs.mountPath }}
          name: {{ .Values.volume.fs.name }}
        - mountPath: {{ .Values.volume.glade.mountPath }}
          name: {{ .Values.volume.glade.name }}
